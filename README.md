Here’s a polished version of your README section for GitHub:

---

# Auto Messages Bot

This bot allows you to send automated messages to a specified channel at regular intervals.

## Commands

* **Start Sending Messages**

```javascript
startAutoMessages()
```

* **Stop Sending Messages**

```javascript
stopAutoMessages()
```

* **Set Target Channel**

```javascript
setChannelId('YOUR_CHANNEL_ID')
```

* **Change Messages**

```javascript
setMessages(['msg1', 'msg2', 'msg3'])
```

* **Change Delay Between Messages**

```javascript
setMessageInterval(15) // Set interval in seconds
```

💡 Tip: Try using `setMessageInterval(10)` to see it work with a shorter interval.

---

If you want, I can also write a **full GitHub-ready README** with **installation instructions, usage examples, and notes** so it looks more professional. Do you want me to do that?
